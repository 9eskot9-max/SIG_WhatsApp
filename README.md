# SIG WhatsApp

Standalone ERPNext integration for sending submitted Sales Invoice and Purchase
Order PDFs through Maytapi. Credentials are configured in the Maytapi Settings
Single DocType and are not stored in source control.

Keep Testing Mode enabled until the test phone receives a document and the
audit log is verified. A successful Maytapi response means queued, not
necessarily delivered.
