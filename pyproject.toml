[project]
name = "sig_whatsapp"
description = "Secure Maytapi WhatsApp document delivery for ERPNext"
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
